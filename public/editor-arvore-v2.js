const auth=firebase.auth(),db=firebase.firestore();
const canvas=document.querySelector('#canvas'),svg=document.querySelector('#linhas'),viewport=document.querySelector('#viewport'),statusEl=document.querySelector('#status'),selPessoa=document.querySelector('#pessoa'),selParente=document.querySelector('#parente');
let pessoas=[],posicoes={},visiveis=new Set(),selecionado='',zoom=1,arrasto=null,tamanho={largura:2400,altura:1600};
const MARGEM=220,EXPANSAO=600,LARGURA=220,ALTURA=120;
const nome=p=>p?`${p.nome} ${p.sobrenome}`:'Pessoa não encontrada',dataBR=d=>d?d.split('-').reverse().join('/'):'',porId=id=>pessoas.find(p=>p.id===id);
function aplicarTamanho(){canvas.style.width=tamanho.largura+'px';canvas.style.height=tamanho.altura+'px';svg.setAttribute('viewBox',`0 0 ${tamanho.largura} ${tamanho.altura}`)}
function parentesDiretos(id){const p=porId(id);if(!p)return[];const ids=new Set([p.paiId,p.maeId,p.conjugeId].filter(Boolean));pessoas.forEach(x=>{if(x.paiId===id||x.maeId===id)ids.add(x.id);if(x.id!==id&&((p.paiId&&x.paiId===p.paiId)||(p.maeId&&x.maeId===p.maeId)))ids.add(x.id)});return pessoas.filter(x=>ids.has(x.id))}
function preencherSelect(select,lista,rotulo){const atual=select.value;select.innerHTML=`<option value="">${rotulo}</option>`;lista.forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=nome(p);select.append(o)});if(lista.some(p=>p.id===atual))select.value=atual}
function atualizarOpcoes(){preencherSelect(selPessoa,selecionado?parentesDiretos(selecionado):pessoas,selecionado?'Selecione um parente direto':'Selecione uma pessoa');preencherSelect(selParente,pessoas.filter(p=>p.id!==selecionado),'Selecione outra pessoa')}
function deslocarTodos(dx,dy){Object.keys(posicoes).forEach(id=>posicoes[id]={x:posicoes[id].x+dx,y:posicoes[id].y+dy})}
function garantirEspaco(x,y){let dx=0,dy=0;if(x<MARGEM)dx=EXPANSAO;if(y<MARGEM)dy=EXPANSAO;if(dx||dy){deslocarTodos(dx,dy);tamanho.largura+=dx;tamanho.altura+=dy;aplicarTamanho();viewport.scrollLeft+=dx*zoom;viewport.scrollTop+=dy*zoom;x+=dx;y+=dy}if(x+LARGURA+MARGEM>tamanho.largura)tamanho.largura+=EXPANSAO;if(y+ALTURA+MARGEM>tamanho.altura)tamanho.altura+=EXPANSAO;aplicarTamanho();return{x,y}}
function padrao(id){const i=[...visiveis].indexOf(id);return garantirEspaco(500+(i%5)*280,500+Math.floor(i/5)*190)}
function render(){aplicarTamanho();canvas.querySelectorAll('.node').forEach(n=>n.remove());document.querySelector('#vazio').style.display=visiveis.size?'none':'block';visiveis.forEach(id=>{const p=porId(id);if(!p)return;const pos=posicoes[id]||padrao(id);posicoes[id]=pos;const n=document.createElement('article');n.className='node'+(id===selecionado?' selecionado':'');n.dataset.id=id;n.style.left=pos.x+'px';n.style.top=pos.y+'px';n.innerHTML=`<span class="sexo">${p.sexo||'—'}</span><strong>${nome(p)}</strong>${p.nascimento?`<span>★ ${dataBR(p.nascimento)}</span>`:''}${p.falecimento?`<span>✝ ${dataBR(p.falecimento)}</span>`:''}${p.cidadeAtual?`<span>${p.cidadeAtual}</span>`:''}`;n.onpointerdown=e=>iniciarArrasto(e,id,n);n.onclick=e=>{e.stopPropagation();selecionado=id;atualizarOpcoes();render()};canvas.append(n)});requestAnimationFrame(desenhar)}
function iniciarArrasto(e,id,el){selecionado=id;atualizarOpcoes();const p=posicoes[id]||padrao(id),r=viewport.getBoundingClientRect();arrasto={id,dx:(e.clientX-r.left+viewport.scrollLeft)/zoom-p.x,dy:(e.clientY-r.top+viewport.scrollTop)/zoom-p.y};svg.style.visibility='hidden';el.setPointerCapture(e.pointerId)}
window.onpointermove=e=>{if(!arrasto)return;const r=viewport.getBoundingClientRect();let x=(e.clientX-r.left+viewport.scrollLeft)/zoom-arrasto.dx,y=(e.clientY-r.top+viewport.scrollTop)/zoom-arrasto.dy;({x,y}=garantirEspaco(x,y));posicoes[arrasto.id]={x,y};const el=canvas.querySelector(`[data-id="${arrasto.id}"]`);if(el){el.style.left=x+'px';el.style.top=y+'px'}};
window.onpointerup=()=>{if(!arrasto)return;arrasto=null;svg.style.visibility='visible';desenhar()};
function path(d,cor){const p=document.createElementNS('http://www.w3.org/2000/svg','path');p.setAttribute('d',d);p.setAttribute('fill','none');p.setAttribute('stroke',cor);p.setAttribute('stroke-width','3');p.setAttribute('stroke-linejoin','round');p.setAttribute('stroke-linecap','round');svg.append(p)}
function linha(a,b,tipo){const A=posicoes[a],B=posicoes[b];if(!A||!B)return;if(tipo==='conjuge'){const E=A.x<=B.x?A:B,D=A.x<=B.x?B:A;path(`M ${E.x+LARGURA} ${E.y+ALTURA/2} H ${D.x}`,'#9a6b5a');return}const x1=A.x+LARGURA/2,y1=A.y+ALTURA,x2=B.x+LARGURA/2,y2=B.y;if(y2<=y1+20){const lado=A.x<=B.x?1:-1;path(`M ${x1} ${y1} H ${x1+lado*55} V ${y2-28} H ${x2-lado*55} V ${y2}`,'#4f7b66');return}const meio=y1+Math.min(Math.max((y2-y1)/2,28),120);path(`M ${x1} ${y1} V ${meio} H ${x2} V ${y2}`,'#4f7b66')}
function desenhar(){svg.innerHTML='';if(arrasto)return;pessoas.forEach(p=>{if(!visiveis.has(p.id))return;if(p.paiId&&visiveis.has(p.paiId))linha(p.paiId,p.id,'pai');if(p.maeId&&visiveis.has(p.maeId))linha(p.maeId,p.id,'mae');if(p.conjugeId&&visiveis.has(p.conjugeId)&&p.id<p.conjugeId)linha(p.id,p.conjugeId,'conjuge')})}
async function relacionar(tipo){if(!selecionado||!selParente.value)return alert('Selecione um quadro e outra pessoa.');const alvo=porId(selecionado),outro=porId(selParente.value);if(!alvo||!outro||alvo.id===outro.id)return;const batch=db.batch();if(tipo==='pai')batch.update(db.collection('pessoas').doc(alvo.id),{paiId:outro.id});if(tipo==='mae')batch.update(db.collection('pessoas').doc(alvo.id),{maeId:outro.id});if(tipo==='conjuge'){batch.update(db.collection('pessoas').doc(alvo.id),{casada:true,conjugeId:outro.id});batch.update(db.collection('pessoas').doc(outro.id),{casada:true,conjugeId:alvo.id})}if(tipo==='filho'){const campo=alvo.sexo==='Feminino'?'maeId':'paiId';batch.update(db.collection('pessoas').doc(outro.id),{[campo]:alvo.id})}if(tipo==='irmao')batch.update(db.collection('pessoas').doc(outro.id),{paiId:alvo.paiId||'',maeId:alvo.maeId||''});await batch.commit();const snap=await db.collection('pessoas').orderBy('nome').get();pessoas=snap.docs.map(d=>({id:d.id,...d.data()}));visiveis.add(outro.id);if(!posicoes[outro.id]){const p=posicoes[alvo.id]||padrao(alvo.id);let nova=tipo==='pai'||tipo==='mae'?{x:p.x+(tipo==='pai'?-260:260),y:p.y-210}:tipo==='conjuge'?{x:p.x+280,y:p.y}:{x:p.x,y:p.y+210};posicoes[outro.id]=garantirEspaco(nova.x,nova.y)}statusEl.textContent='Vínculo salvo.';atualizarOpcoes();render()}
function alinhar(){const nivel=new Map();const calc=(id,pilha=new Set())=>{if(nivel.has(id))return nivel.get(id);if(pilha.has(id))return 0;pilha.add(id);const p=porId(id),n=Math.max(p?.paiId?calc(p.paiId,new Set(pilha))+1:0,p?.maeId?calc(p.maeId,new Set(pilha))+1:0);nivel.set(id,n);return n};[...visiveis].forEach(calc);const grupos={};[...visiveis].forEach(id=>{const n=nivel.get(id)||0;(grupos[n]??=[]).push(id)});const max=Math.max(0,...Object.keys(grupos).map(Number));Object.entries(grupos).forEach(([n,ids])=>ids.forEach((id,i)=>posicoes[id]={x:500+i*280,y:500+(max-Number(n))*210}));render()}
async function salvar(){statusEl.textContent='Salvando desenho…';try{await db.collection('diagramas').doc('arvore-principal').set({posicoes,visiveis:[...visiveis],tamanho,atualizadoEm:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});statusEl.textContent='Desenho salvo no Firebase.'}catch(e){statusEl.textContent='Não foi possível salvar o desenho.'}}
function ajustarZoom(v){zoom=Math.min(1.6,Math.max(.4,v));canvas.style.transform=`scale(${zoom})`;canvas.style.marginRight=(tamanho.largura*(zoom-1))+'px';canvas.style.marginBottom=(tamanho.altura*(zoom-1))+'px';desenhar()}
document.querySelector('#adicionar').onclick=()=>{if(!selPessoa.value)return;visiveis.add(selPessoa.value);selecionado=selPessoa.value;if(!posicoes[selecionado])posicoes[selecionado]=padrao(selecionado);atualizarOpcoes();render()};document.querySelectorAll('[data-rel]').forEach(b=>b.onclick=()=>relacionar(b.dataset.rel));document.querySelector('#remover').onclick=()=>{if(!selecionado)return;visiveis.delete(selecionado);selecionado='';atualizarOpcoes();render()};document.querySelector('#alinhar').onclick=alinhar;document.querySelector('#salvar').onclick=salvar;document.querySelector('#imprimir').onclick=()=>window.print();document.querySelector('#mais').onclick=()=>ajustarZoom(zoom+.1);document.querySelector('#menos').onclick=()=>ajustarZoom(zoom-.1);document.querySelector('#centralizar').onclick=()=>{if(!visiveis.size)return;const xs=[...visiveis].map(id=>posicoes[id]?.x||0),ys=[...visiveis].map(id=>posicoes[id]?.y||0);viewport.scrollTo({left:Math.max(0,((Math.min(...xs)+Math.max(...xs)+LARGURA)/2)*zoom-viewport.clientWidth/2),top:Math.max(0,((Math.min(...ys)+Math.max(...ys)+ALTURA)/2)*zoom-viewport.clientHeight/2),behavior:'smooth'})};canvas.onclick=()=>{selecionado='';atualizarOpcoes();render()};
auth.onAuthStateChanged(async u=>{if(!u){location.href='/';return}const snap=await db.collection('pessoas').orderBy('nome').get();pessoas=snap.docs.map(d=>({id:d.id,...d.data()}));const doc=await db.collection('diagramas').doc('arvore-principal').get();if(doc.exists){const d=doc.data();posicoes=d.posicoes||{};visiveis=new Set(d.visiveis||[]);tamanho=d.tamanho||tamanho}aplicarTamanho();atualizarOpcoes();statusEl.textContent=`${pessoas.length} pessoas cadastradas.`;render();requestAnimationFrame(()=>document.querySelector('#centralizar').click())});